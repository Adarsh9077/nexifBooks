import 'package:flutter/material.dart';

class ItemsStock extends StatelessWidget {
  const ItemsStock({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
