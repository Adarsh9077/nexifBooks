import 'package:flutter/material.dart';

class SalesInvoicesSales extends StatelessWidget {
  const SalesInvoicesSales({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
