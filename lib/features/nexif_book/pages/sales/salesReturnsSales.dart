import 'package:flutter/material.dart';

class SalesReturnsSales extends StatelessWidget {
  const SalesReturnsSales({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
