import 'package:flutter/material.dart';

class CustomersSales extends StatelessWidget {
  const CustomersSales({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
